# orchestration/qortal-skill-promotion-20260914

Contract: /home/iffi/VsCodec-Projects/AI-Orchestration/AGENTS.md
Executing agent (actual executor; never inferred from this template or a role): DeepSeek
  (established from `CODEX_HOME=/home/iffi/.codex-deepseek`, config.toml
  `model = "deepseek-flash"`, `model_provider = "deepseek"`,
  `base_url = https://api.deepseek.com/`, `DEEPSEEK_API_KEY` set, `CODEX_VERSION=0.154.0`).
  The Codex CLI is the harness, not the executor.
Project ID / exact repo / base SHA / agent branch: `orchestration`;
  `/home/iffi/VsCodec-Projects/AI-Orchestration`; base
  `c2cffde7a48b2495a896297c0634f4303a12bc12` (branch
  `agent/orchestration/qortal-skill-promotion-20260914`).
Platform session router / canonical project context: Qortal workspace
  `/home/iffi/VsCodec-Projects/Qortal/qortal-dev-workspace/AGENTS.md`; source of truth is the
  accepted Shadow Archives owner-runtime checkpoint.
Outcome and measurable exit criterion: promote the verified reusable Qortal knowledge into
  `skills/` — one existing skill promoted and four new skills created, all with platform scope,
  maturity, dated pins, freshness gates, failure modes and validation; README index updated;
  validators run; docs branch committed/pushed.
In scope / out of scope / invariants: in scope = `skills/`, `skills/README.md`,
  `skills/shared/capability-harvest/SKILL.md` routing pointer, and this task's orchestration
  artifacts. Out of scope = application source, QDN publication, deploy/release, main merge.
  Invariant = no project-specific Shadow Archives identifiers/UI promoted as universal contracts;
  no Qortal/Qortium contamination; pre-existing owner modifications preserved.
Applicable skills (path / maturity / freshness result): `skills/shared/capability-harvest/SKILL.md`
  (`verified-runtime`, last checked 2026-09-13) — loaded for the promotion decision and routing
  rule; `skills/qortal/cross-app-video-publishing/SKILL.md` (`candidate`) — inventoried as the
  promotion target before rewrite.
Proven reference implementation to reuse (or exact missing investigation delta): accepted Shadow
  Archives Gallery + Video/Q-Tube + Blog/SubWire/Quitter evidence under
  `/home/iffi/VsCodec-Projects/Qortal/qortal-dev-workspace/docs/shadow-archives-webportal/`.
Required surfaces and real runtime acceptance steps: not applicable to this documentation task;
  runtime evidence is inherited from the accepted 2026-09-13 owner-runtime checkpoint
  (Gallery, Video/Q-Tube, Blog/SubWire, optional Quitter = OWNER-RUNTIME PASS). No new runtime
  test is claimed.
Reference freshness evidence / environment IDs: source pins recorded per skill (q-tube
  `68c3ea70`, Subwire `a933a6c4`, Quitter `4e4246c3`, Core `108bf191` = live
  `qortal-6.1.9-108bf19`, Hub `12a573b2`, qapp-core `0f9d6ac5`); live nodes 24991/24992 read-only.
Authorized external actions (exact repo, branch, operation; otherwise none): commit/push the
  documentation branch `agent/orchestration/qortal-skill-promotion-20260914` to
  `git@github.com:iffinland/ai-development-orchestration.git`. No application commit, no QDN
  write, no deploy, no main merge.
Canonical report path / handoff branch: this task directory
  `tasks/orchestration/qortal-skill-promotion-20260914/`; branch as above.
Owner-only steps or reason owner validation is not applicable: documentation-only task; owner
  validation is not applicable (this is bookkeeping of already-accepted evidence).
Capability-harvest expectation (likely reusable capability or project-specific): this task **is**
  the capability harvest for the accepted Shadow Archives verticals.
